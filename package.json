{
  "name": "the-beaconator-api",
  "version": "0.1.0",
  "description": "TypeScript client generation for the-beaconator API",
  "private": true,
  "scripts": {
    "generate:ts": "openapi-typescript openapi.json -o clients/typescript/api.ts",
    "generate:python": "./scripts/generate-python-client.sh",
    "generate": "npm run generate:ts && npm run generate:python"
  },
  "devDependencies": {
    "openapi-typescript": "^7.0.0"
  },
  "dependencies": {
    "openapi-fetch": "^0.12.0"
  }
}
